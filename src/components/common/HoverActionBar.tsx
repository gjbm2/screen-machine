import React from 'react';
import { ImageItem } from '@/types/image-types';

interface HoverActionBarProps {
  image: ImageItem;
}

/**
 * Placeholder – will be replaced with fully featured quick-action menu later.
 */
const HoverActionBar: React.FC<HoverActionBarProps> = () => null;

export default HoverActionBar; 